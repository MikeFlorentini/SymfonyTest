{% extends "base.html.twig" %}

{% block body %}
    <div class="panel-group">
        <div class="panel panel-default">
            <div class="panel-heading">{{ title }}</div>
            <div class="panel-body">
                <form action="/action_page.php">
                    <div class="dropdown">
                        <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Comany
                        <span class="caret"></span></button>
                        <ul class="dropdown-menu">
                            <li><a href="#">Firmen aus DB lesen</a></li>
                        </ul>
                    </div>
                    <div class="form-group">
                        <label for="pwd">Password:</label>
                        <input type="password" class="form-control" id="pwd">
                    </div>
                    <div class="checkbox">
                        <label><input type="checkbox">Testmodus</label>
                    </div>
                    <button type="submit" class="btn btn-default">Exportieren</button>
                </form>
            </div>
            <div class="panel-footer"></div>
        </div>
    </div>
{% endblock %}
